<div class="ui two column centered grid">
  <div class="column">
    <div class="ui raised container segment">
      <%= form_for @user,html:{multipart:true, class: 'ui form'} do |f| %>
      <div class="required field">
        <%= f.label :email %>
        <%= f.email_field :email %>
      </div>

      <div class="required field">
        <%= f.label :password %>
        <%= f.password_field :password %>
      </div>

      <div class="required field">
        <%= f.label "Confirm Password" %>
        <%= f.password_field :password_confirmation %>
      </div>

      <div class="required field">
        <%= f.label :name %>
        <%= f.text_field :name %>
      </div>

      <div class="field">
        <%= f.label :avatar %>
        <!-- <div class="ui padded segment" id="uploader">
          <div id="dragndrop1">
              Drag and Drop an Image Here
          </div>
          <div id="croppie"></div>
        </div>
        <input type="hidden" style="display:none" id="avatar"> -->
        <!-- <#%= f.cl_image_upload :avatar, :id => 'upload_widget_opener' %> -->
        <!-- <p id="upload_widget_opener">Upload multiple images</p> -->
        <!-- <span></span>

        <input type="file" id="upload_widget_opener" accept="image/*"> -->
        <div id="uploader" class="placeholder">
            Drag and Drop Your Image Here to Crop
            <img id="result" src="" />
        </div>
        <!-- <input type="file" name="userprofile_picture" id="filePhoto" /> -->
        <%= f.cl_image_upload :avatar, :id => 'upload', :style => "display:none" %>
      </div>


      <button class="ui purple labeled icon button">
        Create New Account
        <i class="add icon"></i>
      </button>

      <% end %>
    </div>
  </div>
</div>
